// import jwt from "jsonwebtoken";
// export default function UserID(token: string) {
//   const secretKey = process.env.EXPO_PUBLIC_LIOHASH; // Replace this with the actual secret key used on the server
//   // Replace this with the JWT received from the server

//   try {
//     const decodedToken = jwt.verify(token, secretKey);
//     return decodedToken; // This will log the decoded token's payload
//   } catch (error: any) {
//     console.error("Token verification failed:", error.message);
//   }
// }
