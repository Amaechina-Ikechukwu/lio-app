import { View, Text } from "react-native";
import React from "react";
import EditProfile from "../../components/Profile/EditProfile";
import { ErrorBoundary } from "../(home)";

export default function UpdateProfile() {
  return <EditProfile />;
}
